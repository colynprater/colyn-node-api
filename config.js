// Create and exort configuration variables
// Edit routes and response bodies here.

var config = {
  routes: {
    sample: 'hello'
  },
  responseObject: {
    message: 'Hello world'
  }
};

module.exports = config;
